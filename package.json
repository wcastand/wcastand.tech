{
  "name": "wcastand.tech",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn build && gitfolio run",
    "build": "gitfolio build wcastand --sort created --order desc --theme dark",
    "deploy": "cp dist/* . &&now --target=production"
  },
  "dependencies": {
    "gitfolio": "^0.1.4"
  }
}
